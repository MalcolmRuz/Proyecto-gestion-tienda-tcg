package com.gestion.tienda.tcg.registro.enums;

public enum NombreRol {
    ADMIN, // Usuario con permisos como rol "ADMIN",
           // permite usar todos los métodos del SERVICE

    CLIENTE // Usuario limitado como rol "CLIENTE",
            // solo permite registrar como usario en SERVICE

}
